---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="text-slate-500" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('ja-jp', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
